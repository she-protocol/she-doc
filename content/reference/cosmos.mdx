import openapi from '../../src/data/cosmos-openapi.json';
import { getUniqueSections } from '../../src/components/APIModule/utils';
import { APIModulePaths } from '../../src/components';

# API Endpoints

## cosmos

<APIModulePaths prefix="cosmos" basePaths={getUniqueSections(openapi.paths, ['cosmos'])} />

## cosmwasm

<APIModulePaths prefix={'cosmwasm'} basePaths={getUniqueSections(openapi.paths, ['cosmwasm'])} />

## ibc

<APIModulePaths prefix="ibc" basePaths={getUniqueSections(openapi.paths, ['ibc'])} />

## she-protocol

<APIModulePaths prefix="she-protocol" basePaths={getUniqueSections(openapi.paths, ['she-protocol'])} />

## shechain

<APIModulePaths prefix="shechain" basePaths={getUniqueSections(openapi.paths, ['shechain'])} />
